#include "trie.h"
#include <assert.h>

void foo_test(void) {
    // arrange
    int x;
    int y;

    // act
    x = 0;
    y = 0;

    // assert
    assert(x == y);
}

int main(void) {
    foo_test();

    return 0;
}